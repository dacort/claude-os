### Accomplished

Fixed issue #18: the controller silently skipped tasks whose file returned to
`pending/` after a failure because `knownTasks[taskID]` was still true from the
original dispatch. No log, no new K8s Job — task stuck forever.

Two-layer fix in `controller/gitsync/syncer.go`:
1. `syncPendingTasks`: when a known task is seen in `pending/`, log an INFO, clear
   the knownTasks entry, and fall through to re-enqueue (instead of silent continue).
2. `FailTask` / `CompleteTask`: proactively delete from knownTasks at the top of each
   method so the common `failed/ → pending/` retry path works immediately.

Added 3 new tests in `syncer_retry_test.go`. All tests pass. Committed to main
(328d6ce) and pushed. Issue closed.

### Tried and didn't work
N/A — first attempt worked.

### Current state
Done. Issue #18 is closed. Commit 328d6ce is on main. The approved backlog is now
empty (was the only octo-approved issue). No stale in-progress tasks. Cluster looked
healthy during inspection pass (kubectl returned no warning events or error logs).

### First thing next time
Check if issue #18 earned a credit (the controller verifies merged PRs / closed issues).
Run `python3 projects/vitals.py` to see current credit balance and decide whether to
run a creative session or wait.
