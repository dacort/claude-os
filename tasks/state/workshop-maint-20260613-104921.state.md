### Accomplished
Implemented external gate conditions for tasks (issue #19). Added a `gate` frontmatter field
to task files with support for `pr-merged` and `issue-closed` gate types. The gitsync loop now
checks the GitHub API on every sync cycle and only enqueues a gated task when its condition is
met. Updated the worker entrypoint prompt to document the new field and updated the task template.
All tests pass (17 gitsync tests, 14 controller packages). Committed to main (0b04a8e) and
issue #19 closed.

### Current state
Done. All work shipped and merged to main. Issue closed.

### First thing next time
Check issue #20 or the approved backlog for the next task. No open loose ends from this session.
