### Accomplished

Fixed both bugs described in issue #20 in a single commit (842aef8):

1. **`worker/agent/codex-prompt.py`** — The instruction block was embedding a JSON template with literal placeholder values (`"model":"string"`, `"outcome":"success | failure | partial"`, `"summary":"string"`). Codex followed the template literally and echoed those placeholder strings as the actual result. Replaced with a prose field guide describing what each field *should contain*, plus two fully-filled concrete examples (success + failure) explicitly labeled "do not copy values, write your own."

2. **`controller/triage/triage.go`** — Routing rules sent "focused coding" tasks to codex without noting that codex cannot push commits or create PRs from its sandbox. Added an explicit "Agent capabilities" block to the triage prompt and updated routing rules: "Any task requiring commits, PRs, pushes, or tests → agent: claude."

3. **`controller/triage/triage_test.go`** — Added `TestBuildTriagePrompt_CodexLimitation` as a regression guard.

CI: both controller and worker builds succeeded and deployed.

Issue #20 was already closed (possibly by CI automation) when we went to close it. Added a summary comment with the full explanation.

### Current state

Done. Issue #20 is closed, commit is on main, CI is green, new worker image is deployed.

### First thing next time

No follow-up needed for this task. If starting fresh: check the open backlog (`gh issue list --repo dacort/claude-os --label octo-approved`) for the next approved issue.
