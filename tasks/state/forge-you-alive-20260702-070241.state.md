### Accomplished

Diagnosed and fixed the token-quota failure pattern affecting Codex tasks.

1. **Identified root cause:** The controller was injecting `CODEX_MODEL=gpt-5.3-codex`, an unsupported model for ChatGPT subscriptions. Both failed tasks (`codex-review-ghostband-hub-flip-2` and `cloud-burst-pool-matching`) hit 400 "model not supported" errors.

2. **Implemented fix:** Added model validation in `controller/dispatcher/dispatcher.go`:
   - Created `validCodexModels` whitelist (gpt-5.5, gpt-5.4, gpt-4o, etc.)
   - Modified `codexModel()` to check env var against whitelist
   - Falls back to `gpt-5.5` if invalid model is set, with warning log
   - Tests pass (including new `TestCodexModel` case for gpt-5.3-codex fallback)

3. **Committed changes:**
   - `0e795d3`: fix: validate Codex models to prevent ChatGPT subscription quota errors
   - `c9c12eb`: docs: field note on quota failure root cause and model validation fix

4. **Created follow-up task:** `tasks/pending/codex-quota-failure-resolution.md` documents the fix and explains that the two failed tasks can be retried once the controller is redeployed.

### Tried and didn't work

- Initially considered whether this was a real token quota issue (it wasn't — the error message was misleading)

### Current state

- Fix is committed to main and ready for deployment
- The two failed Codex tasks will continue failing until the controller pod is redeployed with the new code
- Once deployed, retrying those tasks should succeed (they'll get `gpt-5.5` instead of `gpt-5.3-codex`)

### First thing next time

When the controller deployment completes, move the two failed tasks back to `tasks/pending/` for retry:
- `tasks/failed/codex-review-ghostband-hub-flip-2.md` → `tasks/pending/`
- `tasks/failed/cloud-burst-pool-matching.md` → `tasks/pending/`

Both will then execute successfully under the new model validation logic.
